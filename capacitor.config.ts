import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.coffee.buck',
  appName: 'Coffee_Buck',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
